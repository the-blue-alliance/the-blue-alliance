// This file is auto-generated by @hey-api/openapi-ts
import { type DefaultError, queryOptions } from '@tanstack/react-query';

import { client } from '../client.gen';
import {
  type Options,
  getInspectionStatus,
  pullEvents,
  pullLiveEventStatus,
  pullPitAddresses,
  pullPitMap,
} from '../sdk.gen';
import type {
  GetInspectionStatusData,
  GetInspectionStatusResponse,
  PullEventsData,
  PullEventsResponse,
  PullLiveEventStatusData,
  PullLiveEventStatusResponse,
  PullPitAddressesData,
  PullPitAddressesResponse,
  PullPitMapData,
  PullPitMapResponse,
} from '../types.gen';

export type QueryKey<TOptions extends Options> = [
  Pick<TOptions, 'baseUrl' | 'body' | 'headers' | 'path' | 'query'> & {
    _id: string;
    _infinite?: boolean;
    tags?: ReadonlyArray<string>;
  },
];

const createQueryKey = <TOptions extends Options>(
  id: string,
  options?: TOptions,
  infinite?: boolean,
  tags?: ReadonlyArray<string>,
): [QueryKey<TOptions>[0]] => {
  const params: QueryKey<TOptions>[0] = {
    _id: id,
    baseUrl:
      options?.baseUrl || (options?.client ?? client).getConfig().baseUrl,
  } as QueryKey<TOptions>[0];
  if (infinite) {
    params._infinite = infinite;
  }
  if (tags) {
    params.tags = tags;
  }
  if (options?.body) {
    params.body = options.body;
  }
  if (options?.headers) {
    params.headers = options.headers;
  }
  if (options?.path) {
    params.path = options.path;
  }
  if (options?.query) {
    params.query = options.query;
  }
  return [params];
};

export const pullLiveEventStatusQueryKey = (
  options: Options<PullLiveEventStatusData>,
) => createQueryKey('pullLiveEventStatus', options);

/**
 * Live event status (pull)
 */
export const pullLiveEventStatusOptions = (
  options: Options<PullLiveEventStatusData>,
) =>
  queryOptions<
    PullLiveEventStatusResponse,
    DefaultError,
    PullLiveEventStatusResponse,
    ReturnType<typeof pullLiveEventStatusQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await pullLiveEventStatus({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: pullLiveEventStatusQueryKey(options),
  });

export const pullPitAddressesQueryKey = (
  options: Options<PullPitAddressesData>,
) => createQueryKey('pullPitAddresses', options);

/**
 * Pit addresses (pull)
 *
 * Get the mapping from team number to pit addresses.
 */
export const pullPitAddressesOptions = (
  options: Options<PullPitAddressesData>,
) =>
  queryOptions<
    PullPitAddressesResponse,
    DefaultError,
    PullPitAddressesResponse,
    ReturnType<typeof pullPitAddressesQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await pullPitAddresses({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: pullPitAddressesQueryKey(options),
  });

export const pullPitMapQueryKey = (options: Options<PullPitMapData>) =>
  createQueryKey('pullPitMap', options);

/**
 * Pit map (pull)
 *
 * Get raw map data that can be used to render your own version of a pit map.
 */
export const pullPitMapOptions = (options: Options<PullPitMapData>) =>
  queryOptions<
    PullPitMapResponse,
    DefaultError,
    PullPitMapResponse,
    ReturnType<typeof pullPitMapQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await pullPitMap({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: pullPitMapQueryKey(options),
  });

export const getInspectionStatusQueryKey = (
  options: Options<GetInspectionStatusData>,
) => createQueryKey('getInspectionStatus', options);

/**
 * Inspection status (pull)
 *
 * Get the mapping from team number to inspection status.
 */
export const getInspectionStatusOptions = (
  options: Options<GetInspectionStatusData>,
) =>
  queryOptions<
    GetInspectionStatusResponse,
    DefaultError,
    GetInspectionStatusResponse,
    ReturnType<typeof getInspectionStatusQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getInspectionStatus({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getInspectionStatusQueryKey(options),
  });

export const pullEventsQueryKey = (options?: Options<PullEventsData>) =>
  createQueryKey('pullEvents', options);

/**
 * Events (pull)
 */
export const pullEventsOptions = (options?: Options<PullEventsData>) =>
  queryOptions<
    PullEventsResponse,
    DefaultError,
    PullEventsResponse,
    ReturnType<typeof pullEventsQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await pullEvents({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: pullEventsQueryKey(options),
  });

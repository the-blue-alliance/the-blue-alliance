// This file is auto-generated by @hey-api/openapi-ts
import { type DefaultError, queryOptions } from '@tanstack/react-query';

import { client } from '../client.gen';
import {
  type Options,
  getEventColors,
  getTeamColors,
  getTeamColorsBatch,
} from '../sdk.gen';
import type {
  GetEventColorsData,
  GetEventColorsResponse,
  GetTeamColorsBatchData,
  GetTeamColorsBatchResponse,
  GetTeamColorsData,
  GetTeamColorsResponse,
} from '../types.gen';

export type QueryKey<TOptions extends Options> = [
  Pick<TOptions, 'baseUrl' | 'body' | 'headers' | 'path' | 'query'> & {
    _id: string;
    _infinite?: boolean;
    tags?: ReadonlyArray<string>;
  },
];

const createQueryKey = <TOptions extends Options>(
  id: string,
  options?: TOptions,
  infinite?: boolean,
  tags?: ReadonlyArray<string>,
): [QueryKey<TOptions>[0]] => {
  const params: QueryKey<TOptions>[0] = {
    _id: id,
    baseUrl:
      options?.baseUrl || (options?.client ?? client).getConfig().baseUrl,
  } as QueryKey<TOptions>[0];
  if (infinite) {
    params._infinite = infinite;
  }
  if (tags) {
    params.tags = tags;
  }
  if (options?.body) {
    params.body = options.body;
  }
  if (options?.headers) {
    params.headers = options.headers;
  }
  if (options?.path) {
    params.path = options.path;
  }
  if (options?.query) {
    params.query = options.query;
  }
  return [params];
};

export const getEventColorsQueryKey = (options: Options<GetEventColorsData>) =>
  createQueryKey('getEventColors', options);

/**
 * Get colors for all teams at an event
 */
export const getEventColorsOptions = (options: Options<GetEventColorsData>) =>
  queryOptions<
    GetEventColorsResponse,
    DefaultError,
    GetEventColorsResponse,
    ReturnType<typeof getEventColorsQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getEventColors({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getEventColorsQueryKey(options),
  });

export const getTeamColorsQueryKey = (options: Options<GetTeamColorsData>) =>
  createQueryKey('getTeamColors', options);

/**
 * Get colors for a single team
 */
export const getTeamColorsOptions = (options: Options<GetTeamColorsData>) =>
  queryOptions<
    GetTeamColorsResponse,
    DefaultError,
    GetTeamColorsResponse,
    ReturnType<typeof getTeamColorsQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getTeamColors({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getTeamColorsQueryKey(options),
  });

export const getTeamColorsBatchQueryKey = (
  options?: Options<GetTeamColorsBatchData>,
) => createQueryKey('getTeamColorsBatch', options);

/**
 * Get colors for multiple teams, or all teams
 *
 * Pass one or more `team` query params to fetch specific teams, or `all` to fetch every team in the system.
 */
export const getTeamColorsBatchOptions = (
  options?: Options<GetTeamColorsBatchData>,
) =>
  queryOptions<
    GetTeamColorsBatchResponse,
    DefaultError,
    GetTeamColorsBatchResponse,
    ReturnType<typeof getTeamColorsBatchQueryKey>
  >({
    queryFn: async ({ queryKey, signal }) => {
      const { data } = await getTeamColorsBatch({
        ...options,
        ...queryKey[0],
        signal,
        throwOnError: true,
      });
      return data;
    },
    queryKey: getTeamColorsBatchQueryKey(options),
  });
